# Name used by `phala deploy -n`
PHALA_CVM_NAME=jupyter-notebook

# Notebook login token shown by Jupyter login page.
JUPYTER_TOKEN=change-me-notebook-token

# Optional fallback if Phala CLI does not return public URL.
# Example: dstack-pha-prod9.phala.network
PHALA_DSTACK_HOST_SUFFIX=
